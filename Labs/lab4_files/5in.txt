void heap_sort(int *arr, size_t sz)
{
	heap_t heap = {.arr = arr, .sz = sz};
	heap_build(&heap);
	for (size_t i = sz - 1; i > 0; i--) {
		int temp = arr[0];
		arr[0] = arr[i];
		arr[i] = temp;
		heap.sz--;
		heap_heapify(&heap, 0);
	}
}
